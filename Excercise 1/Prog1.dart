void main() {
  int ramSize = 4;
  if (ramSize >= 8) {
    print("can run a flutter porject");
  } else if (ramSize < 8) {
    print("can't run a project");
  } else {
    print("invalid input");
  }
}
